{
  "id": "runtime-core-execute-triage",
  "description": "Execute triage scenario for malformed ABC handling in runtime_core.",
  "suites": ["full"],
  "targetRepo": "arkcompiler_runtime_core",
  "surface": "execute",
  "taskShape": "readonly",
  "isolationMode": "worktree",
  "requiresAuth": true,
  "invocation": {
    "type": "execute",
    "taskType": "triage",
    "workItemTitle": "Triage malformed ABC validation issue",
    "summary": "Analyze the impact area for stricter malformed ABC input validation in runtime_core.",
    "issueBody": "Ground the triage report in verifier, assembler, and related docs or tests. No code changes are allowed.",
    "extraInstructions": [
      "Your first response must include at least one `delegate` call with `agent_type` set to `explore` before any direct file reads or narrative text.",
      "Call out verifier and assembler responsibilities explicitly.",
      "Mention the next concrete follow-up area for implementation."
    ],
    "maxIterations": 14,
    "reasoning": "low"
  },
  "expectedArtifacts": ["triage-report.md"],
  "assertions": [
    {
      "type": "contains",
      "source": "artifact",
      "path": "triage-report.md",
      "value": "verifier"
    },
    {
      "type": "contains",
      "source": "artifact",
      "path": "triage-report.md",
      "value": "assembler"
    },
    {
      "type": "matches",
      "source": "repoStatus",
      "pattern": "^\\s*$"
    }
  ],
  "verificationCommands": [],
  "cleanupPolicy": "destroy",
  "requiredToolCalls": [
    {
      "tool": "delegate",
      "minCalls": 1
    }
  ]
}
