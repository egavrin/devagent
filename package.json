{
  "name": "devagent-ts",
  "version": "0.2.3",
  "private": true,
  "packageManager": "bun@1.3.10",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/egavrin/devagent.git"
  },
  "bugs": {
    "url": "https://github.com/egavrin/devagent/issues"
  },
  "homepage": "https://github.com/egavrin/devagent#readme",
  "engines": {
    "bun": ">=1.3.10",
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@devagent-sdk/schema": "file:../devagent-sdk/packages/schema",
    "@devagent-sdk/types": "file:../devagent-sdk/packages/types",
    "@devagent-sdk/validation": "file:../devagent-sdk/packages/validation",
    "cli-highlight": "^2.1.11"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:bundle-smoke": "bun run scripts/smoke-publish-bundle.ts",
    "test:live-validation": "bun test scripts/live-validation/*.test.ts",
    "lint": "turbo run lint",
    "lint:arkts": "turbo run lint:arkts",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "embed": "bun run scripts/embed-assets.ts",
    "bundle": "bun run scripts/bundle.ts",
    "build:publish": "bun run build && bun run bundle",
    "verify:publish": "bun run build:publish && bun run test:bundle-smoke",
    "install-cli": "cd packages/cli && bun link",
    "install-lsp": "bash scripts/install-lsp-servers.sh",
    "check:oss": "node ./scripts/check-oss.mjs",
    "validate:live:smoke": "bun run scripts/live-validation.ts --suite smoke",
    "validate:live:full": "bun run scripts/live-validation.ts --suite full",
    "validate:live:scenario": "bun run scripts/live-validation.ts --scenario",
    "validate:live:execute-deep": "bun run scripts/live-validation/execute-deep.ts",
    "validate:live:execute-chain": "bun run scripts/live-validation/execute-chain.ts",
    "validate:live:provider-smoke": "bun run scripts/live-validation/provider-smoke.ts",
    "validate:live:tui": "bun run scripts/live-validation/tui-validator.ts"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.10",
    "@typescript-eslint/parser": "^8.56.1",
    "turbo": "^2.5.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "minimatch@^3": "3.1.3"
  }
}
